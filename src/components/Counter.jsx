import React from "react";

export const Counter = (props) => {

    const increment = () => {
        let state = "";
        state.map(_ => _);
    }

    let str = "";

    return <div>
        <h3>Counter</h3>
        <button onClick={increment}>Increment</button>
        <p>{str.map(_ => _)}</p>
    </div>
}